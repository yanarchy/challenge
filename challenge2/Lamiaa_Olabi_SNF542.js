// FAMILY

/* TODO:

  Use JS to represent 2 people in your family. Please include the below:
  - Name
  - Relationship to you
  - Age
  - Occupation

*/

var family = ["Name", "Relationship", Age, "Occupation"];
var family = {
	Name: "Khalid",
	Relationship: "Father",
	Age: 56,
	Occupation: "Freelance"
}

				
				 // Here is something to start with

// RANGE

function range (start, end) {
  	var count = 0;
    for (i=0; i <= ; i++){
    	

  }
}

range(0, 4); // => [0, 1, 2, 3]
range(2, 7); // => [2, 3, 4, 5, 6]
range(10, 10); // => []
range(10, 2); // => []

// LAST

function last (arr) {
	 arr.slice(-1).pop;
	 return arr;
}

// ADD ONE

function addOne (arr) {
  for (i=0; i<= arr.length; i++){
  	arr[i]= arr.length + 1;
   }
   return arr;
}

